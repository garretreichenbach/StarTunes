package thederpgamer.startunes.api;

import se.michaelthelin.spotify.SpotifyApi;
import thederpgamer.startunes.StarTunes;

/**
 * Handles Spotify API requests.
 *
 * @author TheDerpGamer
 */
public class SpotifyAPIHandler {

	private static SpotifyApi spotifyApi;

	public static void initialize(StarTunes instance) {
//		try {
//			spotifyApi = new SpotifyApi.Builder().setAccessToken(instance.getSpotifyAccessToken()).build();
//		} catch(Exception exception) {
//			instance.logException(exception.getMessage(), exception);
//		}
	}
}
